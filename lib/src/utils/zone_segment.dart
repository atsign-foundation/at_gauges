import 'package:flutter/material.dart';

class ZoneSegment {
  final double size;
  final Color color;

  ZoneSegment({
    required this.size,
    required this.color
  });

  // ZoneSegment copyWith({
  //   double? size,
  //   Color? color,
  // }) {
  //   return ZoneSegment(
  //     size: size ?? this.size,
  //     color: color ?? this.color,
  //   );
  // }
}